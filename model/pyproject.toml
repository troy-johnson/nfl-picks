[project]
name = "nfl-picks-model"
version = "0.1.0"
description = "NFL pick'em projection pipeline"
requires-python = ">=3.12"
dependencies = ["numpy>=2.1", "pandas>=2.2", "scikit-learn>=1.6"]

[dependency-groups]
dev = ["pytest>=8.3"]
