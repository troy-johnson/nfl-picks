import picksJson from "@/public/data/current.json";
import {GameCard} from "@/components/GameCard";
import type {PicksPayload} from "@/lib/types";
const picks=picksJson as PicksPayload;
function fmt(v:string){if(!v)return "Not generated yet";return new Intl.DateTimeFormat("en-US",{dateStyle:"medium",timeStyle:"short",timeZone:"America/Denver"}).format(new Date(v));}
export default function Home(){const games=[...picks.games].sort((a,b)=>`${a.gameday}T${a.gametime??"23:59"}`.localeCompare(`${b.gameday}T${b.gametime??"23:59"}`));return <main className="page-shell"><header className="page-header"><div><div className="eyebrow">{picks.season||2026} pick'em</div><h1>NFL Picks</h1><p>Market-informed, stats-driven straight-up win probabilities.</p></div><div className="week-badge">Week {picks.week||"—"}</div></header><section className="summary-bar"><div><span>Updated</span><strong>{fmt(picks.generatedAt)}</strong></div><div><span>Market weight</span><strong>{Math.round((picks.model?.marketWeight??0)*100)}%</strong></div><div><span>Validation</span><strong>{picks.model?.validationAccuracy==null?"—":`${Math.round(picks.model.validationAccuracy*100)}%`}</strong></div></section>{games.length?<section className="game-grid">{games.map(g=><GameCard key={g.gameId} game={g}/>)}</section>:<section className="empty-state"><h2>No generated picks yet</h2><p>Run the prediction workflow to populate this week.</p></section>}<section className="legend"><h2>How to read a card</h2><dl><dt>Pick</dt><dd>The team with the higher blended win probability. The blend is the stat model and the market, weighted by the market weight shown above. The percentage next to the pick is that blended probability.</dd><dt>Stat model</dt><dd>Win probability for the pick from a logistic regression on each team&apos;s rolling eight-game offense and defense EPA, success rate, rest days, and neutral-site status. It uses no betting lines.</dd><dt>Market</dt><dd>Win probability for the pick implied by the moneyline after the bookmaker margin (the vig) is removed. This is the strongest single input.</dd><dt>Spread</dt><dd>The favorite and the point spread from the same market.</dd><dt>Crowd</dt><dd>Share of ESPN Pick&apos;em entries that picked this team straight up. It is shown for context and is not a model input. The field is usually more one-sided than the market.</dd><dt>High / Medium / Low</dt><dd>How lopsided the blended probability is. High is 72% or more, Medium is 62% to 71%, Low is under 62%. Low means the game is close to a coin flip, not that the pick is doubtful for other reasons.</dd><dt>Pool pick</dt><dd>Appears only when a pool rule fades the pick: the market has the game within 52/48 and at least 60% of the field is on the favorite. In a small season-long pool, taking the other side costs almost nothing in expected wins and separates you from the field.</dd></dl></section><footer>Rolling EPA + success rate statistical model, blended with de-vigged market probabilities. Crowd is the share of ESPN Pick&apos;em entries on the pick. A pool pick appears only when the rule fades a near coin flip that most of the field is on.</footer></main>}
